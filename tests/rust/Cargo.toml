[package]
name = "integration-tests"
version.workspace = true
edition.workspace = true

[lib]
name = "integration_tests"
bench = false
doctest = false

[dependencies]
common = { path = "../../common" }
