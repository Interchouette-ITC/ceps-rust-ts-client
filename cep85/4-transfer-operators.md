# Transfer / operators

Public `transfer` / `batch_transfer` call on-chain `transfer_from` / `batch_transfer_from`.

`set_approval_for_all` / `is_approved_for_all` use the `operators` dictionary (blake2b of owner‖operator keys).
