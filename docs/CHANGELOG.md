# Changelog

## Unreleased

- Rename Hub/GHCR MCP image from `ceps-client-mcp` to `ceps-rust-ts-client-mcp` (product name). Crate/binary remain `ceps-client-mcp`.

## 1.0.0

- Unified `ceps-client` facades for CEP-18, CEP-78, CEP-85, and CEP-95 on `casper-rust-wasm-sdk` 2.2.x
- Clap CLI `ceps-client-cli`, MCP `ceps-client-mcp`, `ceps-client-wasm` JS packs, NCTL live tests, tip contract WASM staging
- CI: `ci-test`, `nightly-test`, Pages, Hub/GHCR CLI images, GitHub Release assets
- Docs hub with per-CEP closets, SDK dependency note, security policy
