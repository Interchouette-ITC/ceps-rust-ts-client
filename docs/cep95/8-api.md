# CEP-95 API

`ceps_client::CEP95Client` - see rustdoc (`cargo doc -p ceps-client`).

Key methods: `install`, `bind_odra_install`, `mint`, `burn`, `transfer_from`, `safe_transfer_from`, `approve`, `revoke_approval`, `approve_for_all`, `revoke_approval_for_all`, `transfer_ownership`, `name`, `symbol`, `total_supply`, `balance_of`, `owner_of`, `get_approved`, `is_approved_for_all`, `token_metadata`, `get_owner`.

WASM: `CEP95Client` in `ceps-client-wasm`. MCP: `ceps95_*` (see [mcp/TOOLS.md](../../mcp/TOOLS.md)).
