# CEP-85 CLI

```bash
ceps cep85 info
ceps cep85 name --contract-hash <hash> [--package-hash <hash>]
ceps cep85 balance --contract-hash <hash> --account <account-hash|entity-...> --id 1
```

Install and mutate flows are available on `Cep85Client` (see examples and the rest of this closet).
