# Generated by Cargo
target/
**/target/
debug/

# Node
node_modules/
tests/ts/node_modules/

# Local tools (Binaryen pin)
.tools/

# Keys / env
*.pem
.env
.github_credentials

# Editor / OS
**/*.rs.bk
*.pdb
.DS_Store

# Nested itc-cursor is its own git clone (see .git/info/exclude)
docs/api-rust/ceps_client/
docs/api-rust/src/
docs/api-rust/index.html
assets/
docker/ceps

# Client JS packs (ceps-client-wasm/pkg, pkg-nodejs) and demo contract
# WASMs (tests/wasm/**/*.wasm) are committed so consumers can use them
# without building (same idea as rustSDK in-tree pkg/).
